namespace Cohort.Web.Auth;

// Intentionally left as a placeholder.
// Authorization redirects for Host/Admin are handled via CookieAuthenticationEvents in Program.cs.
internal static class CohortAuthorizationMiddlewareResultHandler
{
}
